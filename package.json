{
  "name": "learning-objective-loop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run src",
    "test:e2e": "npm run build && playwright test",
    "test:live": "PLAYWRIGHT_BASE_URL=https://learning-objective-loop.sociobot.in playwright test tests/app.spec.ts",
    "verify:live": "node scripts/verify-live.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@types/node": "22.15.3",
    "@playwright/test": "1.58.2",
    "typescript": "5.9.2",
    "vite": "7.3.6",
    "vitest": "3.2.7"
  }
}
